#ifndef GLOBALS_H
#define GLOBALS_H

#include <SFML/System.hpp>

class Global {
public:
	static const int SCENE_WIDTH = 767, WIDTH = 256, HEIGHT = 224, WIN_SCALE = 2, WIN_WIDTH = WIDTH * WIN_SCALE, WIN_HEIGHT = HEIGHT * WIN_SCALE;
};

#endif